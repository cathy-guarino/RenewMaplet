{
  "name": "server",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "vitest": "^5",
    "wrangler": "latest"
  }
}
